name: test

channels:
  - conda-forge
  - defaults

dependencies:

    # Core dependencies
  - python

  - openff-toolkit-base >=0.11.0
  - ambertools
  - rdkit
  - pydantic
  - jinja2

    # Testing
  - pytest
  - pytest-cov
  - codecov
