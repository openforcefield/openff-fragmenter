name: test
channels:
  - openeye
  - rdkit
  - conda-forge

dependencies:

    # Base Depends
  - python
  - setuptools
  - openeye-toolkits
  - rdkit
  - pyyaml
  - postgresql
  - networkx

    # Testing
  - pytest
  - pytest-cov

  # QCFractal and friends
  - qcfractal
  - geometric
  - torsiondrive

    # Pip depends
  - pip:
    - codecov
    - git+git://github.com/openforcefield/cmiles.git@v0.1.5#egg=cmiles
