name: test
channels:
  - openeye
  - rdkit
  - conda-forge
dependencies:

    # Base Depends
  - python
  - setuptools
  - openeye-toolkits
  - rdkit
  - pyyaml

    # Testing
  - pytest
  - pytest-cov

    # QCFractal and friends
  - qcfractal
  - geometric
  - torsiondrive


    # Pip depends
  - pip:
    - codecov
    - git+git://github.com/openforcefield/cmiles.git@v0.1.3#egg=cmiles
