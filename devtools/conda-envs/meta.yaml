name: test

channels:
  - openeye
  - conda-forge
  - defaults

dependencies:

    # Core dependencies
  - python
  - openff-toolkit-base
  - cmiles-base
  - pyyaml
  - networkx

    # Optional dependencies
  - openeye-toolkits

    # Testing
  - pytest
  - pytest-cov
  - codecov
