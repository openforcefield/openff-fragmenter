name: openff-fragmenter-test

channels:
  - conda-forge
dependencies:
    # Core
  - python
  - pydantic =2
  - jinja2

    # OpenFF stack
  - openff-toolkit-base >=0.14.4
  - openff-interchange =0.4

    # Optional tools
  - ambertools
  - rdkit =2024.03.5

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
