name: openff-fragmenter-test

channels:
  - conda-forge
dependencies:
    # Core
  - python
  - pydantic
  - jinja2

    # OpenFF stack
  - openff-toolkit-base >=0.11.0
  - openff-utilities >=0.1.9

    # Optional tools
  - ambertools
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
