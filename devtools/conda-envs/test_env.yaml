name: openff-fragmenter-test

channels:
  - conda-forge
dependencies:
    # Core
  - python
  - pydantic >=1.10.17,<3
  - jinja2

    # OpenFF stack
  - openff-toolkit-base >=0.14.4
  - openff-utilities >=0.1.9

    # Optional tools
  - ambertools
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
