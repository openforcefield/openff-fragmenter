name: openff-fragmenter-test

channels:
  - conda-forge
dependencies:
    # Core
  - python
  - versioningit
  - pydantic =2
  - jinja2

    # OpenFF stack
  - openff-toolkit-base >=0.14.4
  - openff-interchange >=0.4

    # Optional tools
  - ambertools
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
