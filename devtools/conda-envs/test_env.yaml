name: test

channels:
  - conda-forge
  - defaults

dependencies:

    # Core dependencies
  - python

  - openff-toolkit-base >=0.11.0
  - openff-utilities >=0.1.9
  - ambertools
  - rdkit
  - pydantic
  - jinja2

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly
