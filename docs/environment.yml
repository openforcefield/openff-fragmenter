name: fragmenter
channels:
  - openeye
  - rdkit
  - conda-forge

dependencies:

    # Base Depends
  - python
  - setuptools
  - openeye-toolkits
  - rdkit
  - pyyaml
  - networkx
  - pip
  - numpydoc
    # Pip depends
  - pip:
    - git+git://github.com/openforcefield/cmiles.git@v0.1.5#egg=cmiles
